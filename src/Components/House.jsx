import { Box, styled, Typography } from "@mui/material";
import React from "react";
import house1 from "../media/image1.png";
import bathIcon from "../media/bath_icon.png";
import bedIcon from "../media/bed_icon.png";
import spaceIcon from "../media/space_icon.png";

const House = ({ img, price, address, beds, bathrooms, space }) => {
  const HouseBox = styled(Box)(({ theme }) => ({}));

  const InfoBox = styled(Box)(() => ({}));

  const ImgContainer = styled(Box)(() => ({}));

  return <></>;
};

export default House;
